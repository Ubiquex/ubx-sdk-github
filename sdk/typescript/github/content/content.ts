// Code generated by `ubx sdk gen` from github_content. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Content_Author {
  date?: string | Computed<string>;
  /** The email of the author or committer of the commit. You'll receive a `422` status code if `email` is omitted. */
  email: string | Computed<string>;
  /** The name of the author or committer of the commit. You'll receive a `422` status code if `name` is omitted. */
  name: string | Computed<string>;
}

const Content_AuthorFields: FieldMap = {
  date: "date",
  email: "email",
  name: "name",
};

export interface ContentConfig {
  /** The author of the file. Default: The `committer` or the authenticated user if you omit `committer`. */
  author?: Content_Author | Computed<Content_Author>;
  /** The branch name. Default: the repository’s default branch. */
  branch?: string | Computed<string>;
  /** The person that committed the file. Default: the authenticated user. */
  committer?: Content_Author | Computed<Content_Author>;
  /** The new file content, using Base64 encoding. */
  content: string | Computed<string>;
  /** The commit message. */
  message: string | Computed<string>;
  /** **Required if you are updating a file**. The blob SHA of the file being replaced. */
  sha?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  owner: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  repo: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  path: string | Computed<string>;
}

export interface ContentAttrs {
  /** The author of the file. Default: The `committer` or the authenticated user if you omit `committer`. */
  author: Content_Author;
  /** The branch name. Default: the repository’s default branch. */
  branch: string;
  /** The person that committed the file. Default: the authenticated user. */
  committer: Content_Author;
  /** The new file content, using Base64 encoding. */
  content: string;
  /** The commit message. */
  message: string;
  /** **Required if you are updating a file**. The blob SHA of the file being replaced. */
  sha: string;
  /** path parameter, not part of the API's own resource representation */
  owner: string;
  /** path parameter, not part of the API's own resource representation */
  repo: string;
  /** path parameter, not part of the API's own resource representation */
  path: string;
}

export const Content: ResourceBinding<ContentConfig, ContentAttrs> = {
  wireType: "github_content",
  fields: {
    author: {
      wireName: "author",
      kind: "object",
      fields: Content_AuthorFields,
    },
    branch: "branch",
    committer: {
      wireName: "committer",
      kind: "object",
      fields: Content_AuthorFields,
    },
    content: "content",
    message: "message",
    sha: "sha",
    owner: "owner",
    repo: "repo",
    path: "path",
  },
};
