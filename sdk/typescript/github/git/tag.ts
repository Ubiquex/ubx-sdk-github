// Code generated by `ubx sdk gen` from github_git_tag. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Tag_Tagger {
  /** When this object was tagged. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  date?: string | Computed<string>;
  /** The email of the author of the tag */
  email: string | Computed<string>;
  /** The name of the author of the tag */
  name: string | Computed<string>;
}

export interface Tag_Verification {
  payload: string | Computed<string>;
  reason: string | Computed<string>;
  signature: string | Computed<string>;
  verified: boolean | Computed<boolean>;
  verifiedAt: string | Computed<string>;
}

const Tag_TaggerFields: FieldMap = {
  date: "date",
  email: "email",
  name: "name",
};

export interface TagConfig {
  /** The tag message. */
  message: string | Computed<string>;
  /** The SHA of the git object this is tagging. */
  object: string | Computed<string>;
  /** The tag's name. This is typically a version (e.g., "v0.0.1"). */
  tag: string | Computed<string>;
  /** An object with information about the individual creating the tag. */
  tagger?: Tag_Tagger | Computed<Tag_Tagger>;
  /** The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`. */
  type: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  owner: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  repo: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  tagSha: string | Computed<string>;
}

export interface TagAttrs {
  /** The tag message. */
  message: string;
  nodeId: string;
  /** The SHA of the git object this is tagging. */
  object: string;
  sha: string;
  /** The tag's name. This is typically a version (e.g., "v0.0.1"). */
  tag: string;
  /** An object with information about the individual creating the tag. */
  tagger: Tag_Tagger;
  /** The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`. */
  type: string;
  /** URL for the tag */
  url: string;
  verification: Tag_Verification;
  /** path parameter, not part of the API's own resource representation */
  owner: string;
  /** path parameter, not part of the API's own resource representation */
  repo: string;
  /** path parameter, not part of the API's own resource representation */
  tagSha: string;
}

export const Tag: ResourceBinding<TagConfig, TagAttrs> = {
  wireType: "github_git_tag",
  fields: {
    message: "message",
    object: "object",
    tag: "tag",
    tagger: {
      wireName: "tagger",
      kind: "object",
      fields: Tag_TaggerFields,
    },
    type: "type",
    owner: "owner",
    repo: "repo",
    tagSha: "tag_sha",
  },
};
