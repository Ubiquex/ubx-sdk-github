// Code generated by `ubx sdk gen` from github_code_scanning_default_setup. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface ScanningDefaultSetupConfig {
  owner: string | Computed<string>;
  repo: string | Computed<string>;
}

export interface ScanningDefaultSetupAttrs {
  /** Languages to be analyzed. */
  languages: string[];
  owner: string;
  /** CodeQL query suite to be used. */
  querySuite: string;
  repo: string;
  /** Runner label to be used if the runner type is labeled. */
  runnerLabel: string;
  /** Runner type to be used. */
  runnerType: string;
  /** The frequency of the periodic analysis. */
  schedule: string;
  /** Code scanning default setup has been configured or not. */
  state: string;
  /** Threat model to be used for code scanning analysis. Use `remote` to analyze only network sources and `remote_and_local` to include local sources like filesystem access, command-line arguments, database reads, environment variable and standard input. */
  threatModel: string;
  /** Timestamp of latest configuration update. */
  updatedAt: string;
}

export const ScanningDefaultSetup: DataSourceBinding<ScanningDefaultSetupConfig, ScanningDefaultSetupAttrs> = {
  wireType: "github_code_scanning_default_setup",
  fields: {
    owner: "owner",
    repo: "repo",
  },
};
