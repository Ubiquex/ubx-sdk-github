// Code generated by `ubx sdk gen` from github_code_scanning_analysis. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface ScanningAnalysis_Tool {
  /** The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data. */
  guid?: string | Computed<string>;
  /** The name of the tool used to generate the code scanning analysis. */
  name?: string | Computed<string>;
  /** The version of the tool used to generate the code scanning analysis. */
  version?: string | Computed<string>;
}

export interface ScanningAnalysisConfig {
  analysisId: number | Computed<number>;
  owner: string | Computed<string>;
  repo: string | Computed<string>;
}

export interface ScanningAnalysisAttrs {
  analysisId: number;
  /** Identifies the configuration under which the analysis was executed. For example, in GitHub Actions this includes the workflow filename and job name. */
  analysisKey: string;
  /** Identifies the configuration under which the analysis was executed. Used to distinguish between multiple analyses for the same tool and commit, but performed on different languages or different parts of the code. */
  category: string;
  /** The SHA of the commit to which the analysis you are uploading relates. */
  commitSha: string;
  /** The time that the analysis was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`. */
  createdAt: string;
  deletable: boolean;
  /** Identifies the variable values associated with the environment in which this analysis was performed. */
  environment: string;
  error: string;
  /** Unique identifier for this analysis. */
  id: number;
  owner: string;
  /** The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`, `refs/heads/<branch name>` or simply `<branch name>`. */
  ref: string;
  repo: string;
  /** The total number of results in the analysis. */
  resultsCount: number;
  /** The total number of rules used in the analysis. */
  rulesCount: number;
  /** An identifier for the upload. */
  sarifId: string;
  tool: ScanningAnalysis_Tool;
  /** The REST API URL of the analysis resource. */
  url: string;
  /** Warning generated when processing the analysis */
  warning: string;
}

export const ScanningAnalysis: DataSourceBinding<ScanningAnalysisConfig, ScanningAnalysisAttrs> = {
  wireType: "github_code_scanning_analysis",
  fields: {
    analysisId: "analysis_id",
    owner: "owner",
    repo: "repo",
  },
};
