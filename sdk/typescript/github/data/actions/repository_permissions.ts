// Code generated by `ubx sdk gen` from github_actions_repository_permissions. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface RepositoryPermissionsConfig {
  owner: string | Computed<string>;
  repo: string | Computed<string>;
}

export interface RepositoryPermissionsAttrs {
  /** The permissions policy that controls the actions and reusable workflows that are allowed to run. */
  allowedActions: string;
  /** Whether GitHub Actions is enabled on the repository. */
  enabled: boolean;
  owner: string;
  repo: string;
  /** The API URL to use to get or set the actions and reusable workflows that are allowed to run, when `allowed_actions` is set to `selected`. */
  selectedActionsUrl: string;
  /** Whether actions must be pinned to a full-length commit SHA. */
  shaPinningRequired: boolean;
}

export const RepositoryPermissions: DataSourceBinding<RepositoryPermissionsConfig, RepositoryPermissionsAttrs> = {
  wireType: "github_actions_repository_permissions",
  fields: {
    owner: "owner",
    repo: "repo",
  },
};
