// Code generated by `ubx sdk gen` from github_webhook_config. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface ConfigConfig {
}

export interface ConfigAttrs {
  /** The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`. */
  contentType: string;
  insecureSsl: unknown;
  /** If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/enterprise-cloud@latest/webhooks/event-payloads/#delivery-headers). */
  secret: string;
  /** The URL to which the payloads will be delivered. */
  url: string;
}

export const Config: DataSourceBinding<ConfigConfig, ConfigAttrs> = {
  wireType: "github_webhook_config",
  fields: {
  },
};
